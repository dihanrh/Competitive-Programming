#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;

    while (t--)
    {
        long int x, y, p, q;
        cin >> x >> y >> p >> q;
        p = q = 1 ;

        cout << p << " "<< q << " " << x << " " << y << endl ;
    }

    return 0;
}