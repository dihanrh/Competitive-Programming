#include <iostream>
using namespace std;

int main()
{
    //abcdef
    //cdedcb
/*
    int q;
    cin >> q;

    while (q--)
    {
        int sp = 0, count = 0;
        string s, t;
        char a[t.size()] = {0};

        cin >> s >> t;
        int x = 0;
        int ssize = s.size() - 1;
        while (true)
        {
            if (t[0] == s[x])
            {
                sp = x;
                break;
            }
            if (x == ssize && sp == 0)
            {
                cout << "NO" << endl;
                return 0;
            }

            x++;
        }

        for (int i = 0; i < t.size(); i++)
        {
            for (int j = sp; j < s.size(); j++)
            {
                if (t[i] == s[j] || t[i] == s[j + 1] || t[i] == s[j - 1])
                {
                   count ++ ;
                }
            }
        }

      */

    

    return 0;
}